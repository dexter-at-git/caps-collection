<UserControl x:Class="CapsCollection.Silverlight.UI.Modules.Brewery.Views.BottomMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot">
        <Border x:Name="BottomLinksBorder" Style="{StaticResource NavLinksBorderStyle}" >
            <StackPanel  Orientation="Horizontal"  >
                <HyperlinkButton Content="Add new brewery"
                                 Style="{StaticResource LinkStyle}" 
                                 Command="{Binding AddBreweryCommand}"/>

                <HyperlinkButton Content="Reload breweries"
                                  Style="{StaticResource LinkStyle}" 
                                  Command="{Binding ReloadBreweriesCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
